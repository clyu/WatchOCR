<?xml version="1.0" encoding="utf-8"?>
<!-- Auto Backup rules for Android 11 and below (API < 31). The settings
     DataStore holds the Gemini API key, so it must never leave the device. -->
<full-backup-content>
    <exclude domain="file" path="datastore/watchocr_settings.preferences_pb" />
</full-backup-content>
